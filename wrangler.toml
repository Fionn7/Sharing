name = "sharing"
compatibility_date = "2026-06-05"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages configuration
pages_build_output_dir = "dist"

[observability]
enabled = true

# 配置环境变量（敏感信息通过 wrangler pages secret 设置）
[vars]
PORT = "3100"
